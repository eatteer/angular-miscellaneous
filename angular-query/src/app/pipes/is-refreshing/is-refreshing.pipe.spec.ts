import { IsRefreshingPipe } from './is-refreshing.pipe';

describe('IsRefreshingPipe', () => {
  it('create an instance', () => {
    const pipe = new IsRefreshingPipe();
    expect(pipe).toBeTruthy();
  });
});
